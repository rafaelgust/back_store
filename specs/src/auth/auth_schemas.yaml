components:
  schemas:
    Tokenization:
      type: object
      properties:
        access_token:
          type: string
        refresh_token:
          type: string
    AuthChangePassword:
      type: object
      properties:
        password:
          type: string
        new_password:
          type: string